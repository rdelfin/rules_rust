# Code Generation for protobuf using the prost library
load(":toolchain.bzl", "rust_prostgen_toolchain")

package(default_visibility = ["//visibility:private"])

exports_files(["prostgen.bzl"])

toolchain_type(name = "toolchain")
rust_prostgen_toolchain(name = "default-prostgen-toolchain-impl")

